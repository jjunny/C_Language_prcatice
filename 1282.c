#include <stdio.h>
#include <math.h>

int main(void){
  double n;
  int i = 1, k, t, input;
  scanf("%lf", &sqrt(n));
  input = (int)n;
  while(i<=n){
      i++;
  }
  k = input - i*i;
  t = i;
  printf("%d %d", k, t);
}
